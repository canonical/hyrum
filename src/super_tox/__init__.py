"""super-tox: bulk-run checks across many charm repositories.

Placeholder-named project (see README); the package is laid out so that
renaming `super_tox` → `<new_name>` is a single `git mv` plus
search-and-replace of the package import string.
"""

__version__ = '0.0.1.dev0'
