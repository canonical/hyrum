name: dev
base: ubuntu@26.04
sdks:
  - name: project-hyrum
actions:
  all: make all
  format: make format
  lint: make lint ARGS="$*"
  unit: make unit ARGS="$*"
  hyrum: sudo -u hyrum-check env PATH="$PATH" sh -c 'umask 002; exec /opt/hyrum/venv/bin/hyrum "$@"' hyrum "$@"
